<UserControl x:Class="SISTEMA_ACUMULATIVAS.Views.AcumuladosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SISTEMA_ACUMULATIVAS.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#FFF0F4F8"
             Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="Border">
                            <Setter Property="Margin" Value="15,0"/>
                            <Setter Property="Padding" Value="20,10"/>
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </StackPanel.Resources>

                    <TextBlock Text="Umbrales (UMA 2025: $113.14)" FontSize="14" FontWeight="Bold" Foreground="#FF007BFF"/>

                    <Border Background="#FFFFF3CD" BorderBrush="#FFEEBA00" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="ALERTA (IDENTIFICACIÓN / AVISO)" FontSize="10" FontWeight="Bold" Foreground="#664D00"/>
                            <TextBlock Text="8,000 UMAs" FontSize="16" FontWeight="Bold" Foreground="#664D00" HorizontalAlignment="Center"/>
                            <TextBlock Text="$905,120.00 MXN" FontSize="12" Foreground="#664D00" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Button Grid.Column="1" x:Name="btnActualizar" Content="🔄 Actualizar" 
                    Margin="10,0,0,0" Padding="15,0" Background="#FF007BFF" Foreground="White"
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                    Click="btnActualizar_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15" Margin="0,0,5,0">
                <Grid>
                    <TextBlock Text="Top 10 Clientes por Acumulado (Últimos 6 Meses)" FontSize="16" FontWeight="Bold" 
                               Foreground="#FF007BFF" VerticalAlignment="Top"/>

                    <lvc:CartesianChart Series="{Binding SeriesClientes}" LegendLocation="Bottom" Margin="0,30,0,0">
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Monto Acumulado ($)" LabelFormatter="{Binding Formatter}"/>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Cliente" Labels="{Binding LabelsClientes}"/>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="15" Margin="5,0,0,0">
                <Grid>
                    <TextBlock Text="Operaciones por Tipo" FontSize="16" FontWeight="Bold" 
                               Foreground="#FF007BFF" VerticalAlignment="Top"/>

                    <lvc:PieChart Series="{Binding SeriesTipoOperacion}" LegendLocation="Right" Margin="0,30,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Alertas de Acumulación (Clientes cerca del Umbral de Aviso)" 
                           FontSize="16" FontWeight="Bold" Foreground="#FFDC3545" Margin="0,0,0,10"/>

                <DataGrid x:Name="dgAlertas" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNombre}" Width="*"/>
                        <DataGridTextColumn Header="Acumulado (6 Meses)" Binding="{Binding MontoAcumulado, StringFormat=C}" Width="180"/>
                        <DataGridTextColumn Header="% del Umbral" Binding="{Binding PorcentajeUmbral, StringFormat={}{0:P0}}" Width="120"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding EstadoAlerta}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EstadoAlerta}" Value="⚠️ UMBRAL ALCANZADO">
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding EstadoAlerta}" Value="Cerca del Límite">
                                            <Setter Property="Foreground" Value="Orange"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding EstadoAlerta}" Value="Normal">
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Última Actualización" Binding="{Binding UltimaActualizacion, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

    </Grid>
</UserControl>