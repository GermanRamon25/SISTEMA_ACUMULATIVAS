<UserControl x:Class="SISTEMA_ACUMULATIVAS.Views.OperacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SISTEMA_ACUMULATIVAS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="#FFF0F4F8" Loaded="UserControl_Loaded">

    <!-- RECURSOS (ESTILOS MODERNOS importados de ClienteView) -->
    <UserControl.Resources>

        <!-- Estilo General para Campos de Entrada (TextBox) -->
        <Style TargetType="{x:Type TextBox}" x:Key="ModernInputStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border" 
                                CornerRadius="5" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#FFF5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO ESPECÍFICO PARA COMBOBOX -->
        <Style TargetType="{x:Type ComboBox}" x:Key="ModernComboBoxStyle">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Border x:Name="border" 
                                    CornerRadius="5" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    SnapsToDevicePixels="True">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Contenido seleccionado (texto) -->
                                    <ContentPresenter Grid.Column="0" IsHitTestVisible="False" Margin="5,0,0,0"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                                    <!-- ToggleButton simplificado para la flecha -->
                                    <ToggleButton Grid.Column="1" 
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                                  Style="{x:Null}" Focusable="False" 
                                                  Background="Transparent" BorderThickness="0">
                                        <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Fill="#FF666666" 
                                              Margin="0,0,5,0"
                                              RenderTransformOrigin="0.5,0.5"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <!-- Popup para la lista desplegable -->
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   StaysOpen="False" Focusable="False"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                <Border x:Name="dropDownBorder" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinHeight="{TemplateBinding MinHeight}"
                                        Background="White"
                                        BorderBrush="#FFCCCCCC"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        Padding="1">
                                    <ScrollViewer x:Name="ScrollViewer" Margin="0" Style="{x:Null}">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo Moderno para Botones de Acción -->
        <Style x:Key="ModernActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <Border.Effect>
                                <!-- Sombra sutil para efecto moderno -->
                                <DropShadowEffect Color="#FFBBBBBB" Direction="270" ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Efecto de elevación al pasar el mouse -->
                                <Setter TargetName="Border" Property="Background" 
                                        Value="{Binding Path=Background, RelativeSource={RelativeSource Mode=Self}}"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF888888" Direction="270" ShadowDepth="4" BlurRadius="8" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <!-- Efecto de presión/click -->
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="15">
        <!-- Aumentamos el margen de 10 a 15 para más espacio -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <!-- Aumentamos la proporción a 3* -->
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- COLUMNA IZQUIERDA: TABLA DE OPERACIONES -->
        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <!-- CornerRadius y Padding aumentados -->
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Operaciones Registradas" 
                           FontSize="22" FontWeight="ExtraBold" Foreground="#FF007BFF" Margin="0,0,0,15"/>

                <DataGrid x:Name="dgOperaciones" AutoGenerateColumns="False" 
                          IsReadOnly="True" SelectionMode="Single"
                          SelectionChanged="dgOperaciones_SelectionChanged"
                          BorderThickness="1"
                          BorderBrush="#FFDDDDDD"
                          RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Folio" Binding="{Binding FolioEscritura}" Width="100"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNombre}" Width="*"/>
                        <DataGridTextColumn Header="Tipo Operación" Binding="{Binding TipoOperacion}" Width="150"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat='C'}" Width="120"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaOperacion, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Column="0" Width="5" HorizontalAlignment="Right" Background="Transparent"/>

        <!-- COLUMNA DERECHA: FORMULARIO DE REGISTRO -->
        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="25">
            <!-- CornerRadius y Padding aumentados -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FormularioOperacion">
                    <TextBlock Text="Registrar Operación" 
                               FontSize="22" FontWeight="ExtraBold" 
                               Foreground="#FF007BFF" Margin="0,0,0,30"/>
                    <!-- FontSize y Margin aumentados -->

                    <TextBlock Text="Cliente:" FontWeight="Medium" Margin="0,0,0,5"/>

                    <!-- COMBOBOX CON BÚSQUEDA AVANZADA (Aplicamos estilo) -->
                    <ComboBox x:Name="cmbCliente" 
                              Style="{StaticResource ModernComboBoxStyle}"
                              DisplayMemberPath="Nombre" 
                              SelectedValuePath="Id"
                              IsEditable="True" 
                              IsTextSearchEnabled="False"
                              StaysOpenOnEdit="True"
                              TextSearch.TextPath="Nombre"
                              KeyUp="cmbCliente_KeyUp"/>

                    <ComboBox x:Name="cmbTipoOperacion" 
                              Style="{StaticResource ModernComboBoxStyle}">
                        <ComboBoxItem Content="Transmisión o constitución de derechos reales sobre inmuebles. 8000 UMA;"/>
                        <ComboBoxItem Content="Otorgamiento de poderes para actos de administracion o dominio irrevocables."/>
                        <ComboBoxItem Content="Modificación patrimonial derivada de aumento o disminución de capital social."/>
                        <ComboBoxItem Content="Fusión o escisión de sociedades."/>
                        <ComboBoxItem Content="Compraventa de acciones y partes sociales."/>
                        <ComboBoxItem Content="Constitución o modificación de fideicomisos. 4000 UMA."/>
                        <ComboBoxItem Content="Otra Actividad Vulnerable"/>
                    </ComboBox>

                    <TextBlock Text="Monto de la Operación:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtMonto" 
                             Style="{StaticResource ModernInputStyle}"
                             PreviewTextInput="txtMonto_PreviewTextInput"/>

                    <TextBlock Text="Fecha de Operación:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <!-- Aunque DatePicker no usa Style en este caso, se beneficia de los márgenes -->
                    <DatePicker x:Name="dpFechaOperacion" 
                                Margin="0,5,0,10" Padding="5"/>

                    <TextBlock Text="No. Escritura:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFolioEscritura" 
                             Style="{StaticResource ModernInputStyle}"/>

                    <TextBlock Text="Descripción (Opcional):" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDescripcion" Height="60" TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             Style="{StaticResource ModernInputStyle}"/>
                    <!-- Aplicamos estilo para bordes redondeados -->

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Botón Nuevo (Aplicamos estilo) -->
                        <Button x:Name="btnNuevo" Content="Nuevo" 
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF007BFF" Foreground="White"
                                Click="btnNuevo_Click"/>

                        <!-- Botón Guardar (Aplicamos estilo) -->
                        <Button x:Name="btnGuardar" Content="Guardar" 
                                Grid.Column="1" 
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF28A745" Foreground="White"
                                Click="btnGuardar_Click"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>