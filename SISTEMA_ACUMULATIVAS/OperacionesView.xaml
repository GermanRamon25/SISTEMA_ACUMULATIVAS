<UserControl x:Class="SISTEMA_ACUMULATIVAS.Views.OperacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SISTEMA_ACUMULATIVAS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="#FFF0F4F8" Loaded="UserControl_Loaded">

    <UserControl.Resources>

        <!-- Estilo General para Campos de Entrada (TextBox) -->
        <Style TargetType="{x:Type TextBox}" x:Key="ModernInputStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border" 
                                CornerRadius="5" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#FFF5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO MODERNO PARA ELEMENTOS DEL COMBOBOX -->
        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#EBF3FF"/>
                                <Setter Property="Foreground" Value="#007BFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#007BFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO COMBOBOX BASE CON PLACEHOLDER -->
        <Style TargetType="{x:Type ComboBox}" x:Key="ModernOperacionesComboBoxStyle">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Border x:Name="Border" 
                                    Background="White" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="32"/>
                                    </Grid.ColumnDefinitions>

                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2" 
                                                  Background="Transparent" 
                                                  BorderThickness="0" 
                                                  Focusable="False" 
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                <Border Background="Transparent">
                                                    <Path x:Name="Arrow" 
                                                          Data="M 0 0 L 4 4 L 8 0 Z" 
                                                          Fill="#666666" 
                                                          HorizontalAlignment="Right" 
                                                          VerticalAlignment="Center" 
                                                          Margin="0,0,12,0"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <!-- Placeholder -->
                                    <TextBlock x:Name="PlaceholderText"
                                               Grid.Column="0"
                                               Text="{TemplateBinding Tag}"
                                               Foreground="#999999"
                                               FontStyle="Italic"
                                               VerticalAlignment="Center"
                                               Margin="{TemplateBinding Padding}"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"/>

                                    <ContentPresenter Grid.Column="0" 
                                                      Margin="{TemplateBinding Padding}" 
                                                      VerticalAlignment="Center" 
                                                      HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}" 
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   StaysOpen="False" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" 
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" 
                                        MaxHeight="260" 
                                        Background="White" 
                                        BorderBrush="#CCCCCC" 
                                        BorderThickness="1" 
                                        CornerRadius="5" 
                                        Margin="0,3,0,0"
                                        Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#888888" Direction="270" ShadowDepth="3" BlurRadius="6" Opacity="0.25"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedIndex" Value="-1">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007BFF"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007BFF"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo Moderno para Botones de Acción -->
        <Style x:Key="ModernActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#FFBBBBBB" Direction="270" ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" 
                                        Value="{Binding Path=Background, RelativeSource={RelativeSource Mode=Self}}"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF888888" Direction="270" ShadowDepth="4" BlurRadius="8" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- COLUMNA IZQUIERDA: TABLA DE OPERACIONES -->
        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Operaciones Registradas" 
                           FontSize="22" FontWeight="ExtraBold" Foreground="#FF007BFF" Margin="0,0,0,15"/>

                <DataGrid x:Name="dgOperaciones" AutoGenerateColumns="False" 
                          IsReadOnly="True" SelectionMode="Single"
                          SelectionChanged="dgOperaciones_SelectionChanged"
                          BorderThickness="1"
                          BorderBrush="#FFDDDDDD"
                          RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Folio" Binding="{Binding FolioEscritura}" Width="100"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNombre}" Width="*"/>
                        <DataGridTextColumn Header="Tipo Operación" Binding="{Binding TipoOperacion}" Width="160"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat='C'}" Width="120"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaOperacion, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

        <!-- COLUMNA DERECHA: FORMULARIO DE REGISTRO -->
        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="25">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FormularioOperacion">
                    <TextBlock Text="Registrar Operación" 
                               FontSize="22" FontWeight="ExtraBold" 
                               Foreground="#FF007BFF" Margin="0,0,0,30"/>

                    <!-- BÚSQUEDA RÁPIDA DE CLIENTE CON AUTOCOMPLETADO -->
                    <TextBlock Text="Buscar Cliente (Nombre o RFC):" FontWeight="Medium" Margin="0,0,0,5"/>
                    <Grid Margin="0,5,0,5">
                        <TextBox x:Name="txtBuscarCliente"
                                 Style="{StaticResource ModernInputStyle}"
                                 Margin="0"
                                 Height="38"
                                 TextChanged="txtBuscarCliente_TextChanged"
                                 PreviewKeyDown="txtBuscarCliente_PreviewKeyDown"/>

                        <!-- Texto de sugerencia cuando está vacío -->
                        <TextBlock Text="Escriba para buscar por Nombre o RFC..."
                                   Foreground="#999999"
                                   FontStyle="Italic"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=txtBuscarCliente}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Menú flotante de resultados filtrados -->
                        <Popup x:Name="popupClientes" 
                               PlacementTarget="{Binding ElementName=txtBuscarCliente}" 
                               Placement="Bottom" 
                               IsOpen="False" 
                               StaysOpen="False" 
                               AllowsTransparency="True"
                               PopupAnimation="Slide">
                            <Border Background="White" 
                                    BorderBrush="#CCCCCC" 
                                    BorderThickness="1" 
                                    CornerRadius="5" 
                                    MaxHeight="220" 
                                    MinWidth="{Binding ActualWidth, ElementName=txtBuscarCliente}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#888888" Direction="270" ShadowDepth="3" BlurRadius="6" Opacity="0.25"/>
                                </Border.Effect>
                                <ListBox x:Name="lstClientesSugeridos" 
                                         BorderThickness="0"
                                         SelectionChanged="lstClientesSugeridos_SelectionChanged"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="5,4">
                                                <TextBlock Text="{Binding Nombre}" FontWeight="SemiBold" FontSize="13" Foreground="#333333"/>
                                                <TextBlock Text="{Binding RFC, StringFormat='RFC: {0}'}" FontSize="11" Foreground="#007BFF"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- Estado/Confirmación de selección -->
                    <TextBlock x:Name="lblClienteSeleccionado" 
                               Text="Ningún cliente seleccionado" 
                               FontSize="11" 
                               FontStyle="Italic" 
                               Foreground="#666666" 
                               Margin="0,0,0,10"/>

                    <!-- TIPO DE OPERACIÓN NOTARIAL -->
                    <TextBlock Text="Tipo de Operación:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbTipoOperacion" 
                              Tag="Seleccione el tipo de operación..."
                              Style="{StaticResource ModernOperacionesComboBoxStyle}"
                              SelectedIndex="-1"
                              Margin="0,5,0,10"
                              SelectedValuePath="Id"
                              SelectionChanged="cmbTipoOperacion_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Nombre}" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- MONTO -->
                    <TextBlock Text="Monto de la Operación:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtMonto" 
                             Style="{StaticResource ModernInputStyle}"
                             PreviewTextInput="txtMonto_PreviewTextInput"/>

                    <!-- FECHA DE OPERACIÓN -->
                    <TextBlock Text="Fecha de Operación:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dpFechaOperacion" 
                                Margin="0,5,0,10" Padding="5"/>

                    <!-- NO. ESCRITURA -->
                    <TextBlock Text="No. Escritura:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFolioEscritura" 
                             Style="{StaticResource ModernInputStyle}"/>

                    <!-- DESCRIPCIÓN -->
                    <TextBlock Text="Descripción (Opcional):" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDescripcion" Height="60" TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             Style="{StaticResource ModernInputStyle}"/>

                    <!-- BOTONES DE ACCIÓN -->
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Botón Nuevo -->
                        <Button x:Name="btnNuevo" Content="Nuevo" 
                                Grid.Column="0"
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF007BFF" Foreground="White"
                                Click="btnNuevo_Click"/>

                        <!-- Botón Guardar -->
                        <Button x:Name="btnGuardar" Content="Guardar" 
                                Grid.Column="1" 
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF28A745" Foreground="White"
                                Click="btnGuardar_Click"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>