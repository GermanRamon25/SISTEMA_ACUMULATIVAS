<UserControl x:Class="SISTEMA_ACUMULATIVAS.Views.ClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SISTEMA_ACUMULATIVAS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="#FFF0F4F8" Loaded="UserControl_Loaded">

    <!-- RECURSOS (ESTILOS MODERNOS) -->
    <UserControl.Resources>

        <!-- Estilo General para Campos de Entrada (TextBox) -->
        <Style TargetType="{x:Type TextBox}" x:Key="ModernInputStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border" 
                                CornerRadius="5" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007BFF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#FFF5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO MODERNO PARA ELEMENTOS DEL COMBOBOX -->
        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#EBF3FF"/>
                                <Setter Property="Foreground" Value="#007BFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#007BFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO MODERNO PARA EL COMBOBOX CON PLACEHOLDER -->
        <Style TargetType="{x:Type ComboBox}" x:Key="ModernComboBoxStyle">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Border x:Name="Border" 
                                    Background="White" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="32"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Botón transparente que abre el desplegable -->
                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2" 
                                                  Background="Transparent" 
                                                  BorderThickness="0" 
                                                  Focusable="False" 
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                <Border Background="Transparent">
                                                    <Path x:Name="Arrow" 
                                                          Data="M 0 0 L 4 4 L 8 0 Z" 
                                                          Fill="#666666" 
                                                          HorizontalAlignment="Right" 
                                                          VerticalAlignment="Center" 
                                                          Margin="0,0,12,0"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <!-- Texto de marca de agua / Placeholder desvanecido -->
                                    <TextBlock x:Name="PlaceholderText"
                                               Grid.Column="0"
                                               Text="Seleccione el tipo de persona..."
                                               Foreground="#999999"
                                               FontStyle="Italic"
                                               VerticalAlignment="Center"
                                               Margin="{TemplateBinding Padding}"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"/>

                                    <!-- Texto seleccionado -->
                                    <ContentPresenter Grid.Column="0" 
                                                      Margin="{TemplateBinding Padding}" 
                                                      VerticalAlignment="Center" 
                                                      HorizontalAlignment="Left" 
                                                      IsHitTestVisible="False" 
                                                      Content="{TemplateBinding SelectionBoxItem}" 
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                </Grid>
                            </Border>

                            <!-- Desplegable ajustado al ancho total -->
                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   StaysOpen="False" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" 
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                        Background="White" 
                                        BorderBrush="#CCCCCC" 
                                        BorderThickness="1" 
                                        CornerRadius="5" 
                                        Margin="0,3,0,0" 
                                        Padding="3">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#888888" Direction="270" ShadowDepth="3" BlurRadius="6" Opacity="0.25"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedIndex" Value="-1">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007BFF"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007BFF"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo Moderno para Botones de Acción -->
        <Style x:Key="ModernActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#FFBBBBBB" Direction="270" ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" 
                                        Value="{Binding Path=Background, RelativeSource={RelativeSource Mode=Self}}"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF888888" Direction="270" ShadowDepth="4" BlurRadius="8" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- Contenedor Principal Izquierdo: Lista, Búsqueda e Importación -->
        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Buscar por Nombre o RFC:" 
                               VerticalAlignment="Center" 
                               FontWeight="Medium"
                               Foreground="#FF333333"/>

                    <TextBox x:Name="txtBusqueda" 
                             Width="260" 
                             Style="{StaticResource ModernInputStyle}"
                             Margin="12,0,0,0" 
                             TextChanged="txtBusqueda_TextChanged"/>

                    <!-- BOTÓN DE IMPORTACIÓN EXCEL / CSV -->
                    <Button x:Name="BtnImportarClientes" 
                            Content="📂 Importar Excel / CSV" 
                            Height="38" Width="190"
                            Background="#0F766E" Foreground="White" 
                            FontWeight="SemiBold" BorderThickness="0"
                            Cursor="Hand"
                            Margin="15,0,0,0"
                            Click="BtnImportarClientes_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <!-- DataGrid de Clientes sin el encabezado de fila lateral -->
                <DataGrid x:Name="dgClientes" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          SelectionChanged="dgClientes_SelectionChanged"
                          BorderThickness="1"
                          BorderBrush="#FFDDDDDD"
                          RowHeight="30">
                    <DataGrid.Columns>
                        <!-- <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/> -->
                        <DataGridTextColumn Header="Otorgante" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="RFC" Binding="{Binding RFC}" Width="130"/>
                        <DataGridTextColumn Header="CURP" Binding="{Binding CURP}" Width="150"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding TipoPersonaDisplay}" Width="100"/>
                        <DataGridTextColumn Header="Fecha Registro" Binding="{Binding FechaRegistro, StringFormat='dd/MM/yyyy'}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

        <!-- Contenedor Principal Derecho: Formulario de Detalles -->
        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="25">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FormularioCliente">
                    <TextBlock Text="Detalles del Cliente" 
                               FontSize="22" 
                               FontWeight="ExtraBold" 
                               Foreground="#D4A843" 
                               Margin="0,0,0,30"/>

                    <!-- ID Cliente -->
                    <TextBlock Text="ID Cliente:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtId" 
                             IsReadOnly="True" 
                             Style="{StaticResource ModernInputStyle}"
                             Background="#FFF5F5F5"/>

                    <!-- Nombre Completo / Razón Social -->
                    <TextBlock Text="Nombre Completo / Razón Social:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtNombre" 
                             Style="{StaticResource ModernInputStyle}"/>

                    <!-- RFC (Único) - Con evento TextChanged -->
                    <TextBlock Text="RFC (Único):" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtRFC" 
                             MaxLength="13" 
                             CharacterCasing="Upper" 
                             Style="{StaticResource ModernInputStyle}"
                             PreviewKeyDown="TxtSinEspacios_PreviewKeyDown"
                             TextChanged="txtRFC_TextChanged"/>

                    <!-- CURP (Opcional) -->
                    <TextBlock Text="CURP (Opcional):" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtCURP" 
                             MaxLength="18" 
                             CharacterCasing="Upper" 
                             Style="{StaticResource ModernInputStyle}"
                             PreviewKeyDown="TxtSinEspacios_PreviewKeyDown"/>

                    <!-- Tipo de Persona - Con evento SelectionChanged -->
                    <TextBlock Text="Tipo de Persona:" FontWeight="Medium" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbTipoPersona" 
                              Style="{StaticResource ModernComboBoxStyle}" 
                              Margin="0,5,0,25"
                              SelectedIndex="-1"
                              SelectionChanged="cmbTipoPersona_SelectionChanged">
                        <ComboBoxItem Content="Persona Física" Tag="F"/>
                        <ComboBoxItem Content="Persona Moral" Tag="M"/>
                    </ComboBox>

                    <!-- GRUPO DE BOTONES DE ACCIÓN -->
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Botón Nuevo -->
                        <Button x:Name="btnNuevo" 
                                Content="Nuevo" 
                                Grid.Column="0"
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF007BFF" 
                                Foreground="White" 
                                Click="btnNuevo_Click"/>

                        <!-- Botón Guardar -->
                        <Button x:Name="btnGuardar" 
                                Content="Guardar" 
                                Grid.Column="1" 
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FF28A745" 
                                Foreground="White" 
                                Click="btnGuardar_Click"/>

                        <!-- Botón Eliminar/Desactivar -->
                        <Button x:Name="btnEliminar" 
                                Content="Desactivar" 
                                Grid.Column="2" 
                                Style="{StaticResource ModernActionButtonStyle}"
                                Background="#FFDC3545" 
                                Foreground="White" 
                                Click="btnEliminar_Click"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>